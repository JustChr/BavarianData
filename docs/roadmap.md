# BavarianData roadmap

Where this integration is going and why. Written 2026-07-23, against v0.8.1.

## Why we need a direction

Two facts set the stage:

1. **`bimmer_connected` is dead.** BMW blocked third-party access to the MyBMW
   backend on 2025-09-29; the upstream custom component now redirects users to
   CarData. The whole BMW/Home Assistant userbase is migrating onto our channel.
2. **The CarData space is crowded and undifferentiated.** JjyKsi, kvanbiesen,
   koenraadhaedens, jz-v and others all do the same thing: descriptor in,
   entity out. We do it better — curated bilingual names, cluster picker,
   bundled card, derived energy sensors — but "best pipe" is a lead, not a moat.

Every competing integration is **stateless**: it shows you *now*. BMW's own app
discards detail, Home Assistant's recorder purges after ten days, and long-term
statistics keep only hourly means. Nobody turns the stream into a *record*, and
nobody turns that record into *decisions*.

**That's the opportunity: BavarianData remembers your car.** Trips, charging
sessions and battery health, derived from a stream that costs no API quota,
persisted independently of the recorder. History compounds — after six months a
user's data lives here, and a competitor cannot retro-fit it.

## UX doctrine

We ship ~295 possible descriptor entities already. The fastest way to ruin this
integration is to bolt an analytics product onto that entity flood. Five rules:

1. **One entity per question a user actually asks** — not one per data point.
   "What did charging cost me this month?" is an entity. "Session 47's grid kWh"
   is an attribute.
2. **No new setup steps.** Onboarding is already client ID → device auth →
   cluster picker → browser-console snippet. Features self-enable when their data
   appears and self-explain when it doesn't.
3. **The card is the UI.** Home Assistant's entity list cannot render a session
   list or a power curve. History features live as card views or they don't exist.
4. **Never show a number we aren't sure of.** No cost before a tariff is
   configured. No battery-health percentage before we have enough samples.
   "Learning (3/10)" beats a figure that jumps.
5. **Privacy is a visible choice, not a buried option.**

## Phases

Each phase is independently releasable. We can stop after any of them without
leaving scaffolding behind.

| Phase | Scope | Status |
| --- | --- | --- |
| 0 | History store + derived-entity translations | built; store not yet wired into setup |
| 1 | Charging ledger and real cost | next |
| 2 | Battery health / degradation | sketched |
| 3 | Trips (Fahrtenbuch) | sketched |
| 4 | Statistics backfill + export | sketched |

Charging goes first deliberately: it is the smallest delta from what already
exists (sessions are detected today), it touches no GPS so no privacy design
blocks it, cost is the single most-wanted number, and it proves the store at low
risk. Battery health then comes nearly free on top of the session records.

---

## Phase 0 — Foundation

Invisible to users. Two pieces.

### 0.1 History store

A `Store`-backed JSON file, matching the pattern already used by
[`image.py`](../custom_components/bavariandata/image.py) and the request log in
[`__init__.py`](../custom_components/bavariandata/__init__.py). No SQLite, no new
dependency.

- Key `bavariandata_<entry_id>_history`, version 1.
- Volume is small: roughly 365 sessions and 1,100 trips per year. Bounded by a
  retention setting plus a hard cap.
- **Writes are debounced** (`Store.async_delay_save`). We must never write on a
  stream message — they arrive constantly.
- **No GPS traces.** Start and end points only. This keeps the file small *and*
  is the privacy-correct default.

### 0.2 Derived-entity translations

`CardataChargedEnergySensor` and friends set a hardcoded English `_attr_name`
([`sensor.py:507`](../custom_components/bavariandata/sensor.py#L507)), so they
bypass the translation pipeline: German users see "Charged Energy (Total)".
The `entity` block of `translations/*.json` is fully regenerated by
`tools/generate_translations.py`, so keys can't simply be hand-added there.

Every phase below adds more derived entities, so fix the mechanism first: extend
the generator with a project-authored source for non-catalogue entity keys
(alongside `tools/curated_titles.json`) so derived sensors land in the generated
`entity` block in **both** languages, sharing `keys.py` with the runtime the way
catalogue descriptors already do.

Related known gap: `de.json` still has no `config`/`options` sections at all, so
the whole setup flow falls back to English for German users. Phase 1 adds an
options screen — **fix the German options block before, not after.**

---

## Phase 1 — Charging ledger

### What the user gets

Three sensors per VIN, plus one conditional:

| Entity | Notes |
| --- | --- |
| `Charging Cost (This Month)` | `device_class: monetary`, resets monthly |
| `Charging Cost (Last Session)` | `device_class: monetary` |
| `Charging Energy (This Month)` | kWh |
| `Charging Cost per 100 km` | *only created when the odometer descriptor is streaming* |

Cost entities **do not exist at all** until a tariff is configured (rule 4).

A new card view — `view: charging` — lists recent sessions with date, energy,
cost and a home/public badge; tapping one expands to the power curve, the
SoC start→end arc, and duration. The card keeps its existing `cluster:` key
working untouched; `view:` is separate because these aren't catalogue clusters.

A response-returning service `bavariandata.get_charging_sessions`
(`SupportsResponse.ONLY`, optional `vin` / `from` / `to` / `limit`) returns the
full history for templates, scripts and the card. It reads the local store, so
it costs **no API quota**. This is what keeps detail out of the entity registry.

### Session record

Built at the transitions already handled by `_fire_charging_event`
([`coordinator.py:504`](../custom_components/bavariandata/coordinator.py#L504)):
open a record on start, close and persist on stop.

```jsonc
{
  "id": "<vin>-<start_iso>",
  "start": "...", "end": "...", "duration_s": 0,
  "soc_start": 42, "soc_end": 80, "target_soc": 80,
  "energy_kwh": 0.0,        // integrated from the stream (battery side)
  "grid_kwh": null,         // REST enrichment or a bound wallbox entity
  "avg_power_kw": 0.0, "peak_power_kw": 0.0,
  "power_curve": [[0, 11.0], [60, 10.9]],   // downsampled, ~1/min, capped
  "location": { "zone": "home", "lat": null, "lon": null },
  "location_assumed": false,
  "cost": { "amount": 4.21, "currency": "EUR", "source": "tariff" },
  "end_reason": null,
  "enriched": false
}
```

### Cost model

Cost accumulates **incrementally alongside energy**, piggybacking on
`_integrate_energy` ([`coordinator.py:455`](../custom_components/bavariandata/coordinator.py#L455)).
Sampling the price entity live during the session means dynamic tariffs work
without ever querying recorder history.

Priority when closing a session:

1. BMW's own figure, if the session was enriched — `chargingCostInformation`
   carries currency, calculated cost and calculated savings.
2. The integrated tariff cost.
3. Nothing. We show no cost rather than a wrong one.

**Which kWh does cost use?** Our integrated figure is battery-side and
undercounts grid draw by roughly the AC charging losses. Options, in order of
honesty: a bound wallbox energy entity (exact, free, no quota) > BMW's `grid_kwh`
from enrichment > battery-side kWh with an optional user-set loss percentage
(**default 0** — we label what we measured rather than inflating it).

### Location

Home versus public is resolved against **Home Assistant zones** — no
configuration. If the GPS cluster isn't enabled (common, since it's optional),
we fall back to the home tariff and set `location_assumed: true`, which the card
shows as a subdued "assumed home" marker. This is a deliberate, visible
compromise: silently assuming would violate rule 4, and refusing to cost anything
would make the feature useless for the majority who only charge at home.

### Enrichment, not a new service

`bavariandata.fetch_charging_history` currently dumps raw JSON to an event.
Upgrade it to **merge** BMW's data into stored sessions — cost, formatted
address, provider name, grid kWh — matched on a start-time window. One existing
service gets better; no new quota surface, capped at one call per day and
opt-in.

### Options

One new screen, *Energy prices*: mode (none / fixed / entity), fixed price and
currency, price-entity picker, optional wallbox energy entity, optional AC-loss
percentage. We do **not** build a tariff editor — users already have
Tibber/Nordpool/aWATTar.

A second small screen, *History & privacy*: retention (12 / 24 months /
forever), "store locations" toggle, and **Delete all history**.

### Discoverability

After the first session is recorded, **one** persistent notification:
"BavarianData is now keeping your charging history — add the Charging card to see
it." Once, never repeated.

### Code layout

The analytics must be **HA-import-free pure functions** so they're testable in
our existing harness (`tests/conftest.py` deliberately loads modules without
Home Assistant). This also keeps `coordinator.py` — already 935 lines — from
becoming unmaintainable.

```
custom_components/bavariandata/history/
  models.py     # dataclasses + (de)serialisation      — HA-free, tested
  sessions.py   # session state machine                — HA-free, tested
  pricing.py    # cost math                            — HA-free, tested
  store.py      # Store wrapper                        — HA imports live here
```

This package exists as of Phase 0, with `tests/test_history.py` covering the
three HA-free modules. What remains for Phase 1 is the wiring: instantiate
`HistoryStore` during setup, drive a `SessionBuilder` from the transitions in
`_fire_charging_event`, feed `CostAccumulator` from `_integrate_energy`, and
surface the result as sensors, a service and a card view.

Wrong numbers are the one thing that would destroy trust in this feature, and
entity code isn't currently test-covered — so the maths goes where we *can* test
it.

---

## Phase 2 — Battery health

Nearly free once sessions exist: ΔkWh over ΔSoC across many sessions yields a
usable-capacity estimate, tracked against odometer and vehicle age. Cross-check
against `Battery HV Max size`, `maxEnergy` and `Battery health state`.

One sensor with an explicit **confidence model** — below roughly ten good
samples it reads "Learning (n/10)" and never a jumpy percentage. Card gauge plus
a degradation trend. EV owners are obsessive about state of health and there is
no BMW equivalent of the Tesla tooling.

## Phase 3 — Trips (Fahrtenbuch)

The headline differentiator, done last on purpose: it needs the privacy design,
reverse geocoding, and the business/private classification UX, and by then the
store is proven.

Reconstructable entirely from the stream at zero quota cost: `ignition state`,
`engine state`, `motion state`, GPS, mileage, SoC, `Trip last distance`/`time`,
`electric share`, `Range EV average consumption`, driving-style descriptors.

Classification via an actionable notification ("Business / Private") or a
`select`. Export to CSV/PDF.

> **Never claim tax compliance.** A Finanzamt-proof logbook has legal
> tamper-resistance and timeliness requirements we cannot guarantee. This is a
> trip journal and expense helper. Say so in the docs, from day one.

## Phase 4 — Statistics backfill and export

Import charging history and trips into Home Assistant's long-term statistics via
`async_import_statistics`, so the Energy dashboard shows charging from before
install or while HA was down. Plus monthly CSV/PDF reports.

---

## Beyond the roadmap

Ideas worth keeping but not scheduled:

- **Charge planner** — target SoC, departure time (from an HA calendar, or the
  car's own streamed climate-timer descriptors), a price entity and optional
  solar surplus produce `binary_sensor.should_charge_now` and a planned cost.
  We can also read the car's own charging profile and *warn about conflicts*
  ("the car is in price-optimised mode and will refuse your surplus charging").
- **Arrival ETA** — `sensor.minutes_until_home` from live GPS, heading and
  speed. Everyone else has only home/not-home; this is a genuinely new
  automation primitive.
- **Service centre** — project when CBS distance thresholds are actually hit
  using `CBS average distance per day`, and publish a `calendar` entity.
- **Tyre intelligence** — the tyre-diagnosis endpoint carries tread depth, wear,
  season, mounting date and DOT production date. And because we stream *per-wheel
  tyre temperature*, we can do temperature-corrected pressure, which almost no
  other data source can.
- **Watchdogs** — window open with rain forecast; car unlocked away from home;
  12 V voltage trending down over weeks (a classic BMW failure we can see coming).
- **Descriptor coverage self-test** — "you enabled cluster X but descriptor Y has
  never arrived in 7 days". Extendable to an opt-in anonymised model
  compatibility matrix; nobody knows what their car actually streams, and we
  could become the authority on that.

## Standing risks

- **Entity sprawl.** The entity budget above is a cap, not a starting point.
- **Store growth** independent of the recorder — retention must actually be
  enforced, and saves must stay debounced.
- **Quota.** The stream carries these features; REST only garnishes them. Home
  sessions must never need an API call.
- **Bilingual parity.** Every phase adds derived entities; Phase 0.2 exists so
  that doesn't quietly become an English-only surface.
- **Timezone and DST** on monthly cost buckets.
- **Spare-time scope.** Four phases is a lot. Ship each one whole.
